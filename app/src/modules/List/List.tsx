import React from 'react';
import styled from 'styled-components';

export const DemoList = () => (
    <Container> Welcome to the List Page!!</Container>
);

const Container = styled.div`
    margin: 25% auto;
    text-align: center;
    font-size: 2em;
`;
